# here we will make a python list using c array
import ctypes

class ctypes:
    def __init__(self):
        self.size = 1     # size is how many you can store
        self.n = 0        # n = no. of element stores
        self.A = self.__make_array(self.size)
        